<!DOCTYPE html>
<html>
<head>
  <title>Photogur</title>
  <link rel="stylesheet" href="../../assets/stylesheets/styles.css">
  <%= stylesheet_link_tag    'application', media: 'all' %>
  <%= javascript_include_tag 'application' %>
  <%= csrf_meta_tags %>
</head>
<body>
  <header>
    <% if flash[:alert] %>
      <p>Sorry, looks like an error occured!</p>
      <ul>
        <% flash[:alert].each do |message|%>
        <li><%= message %></li>
        <% end %>
      </ul>
    <% end %>
  </header>
  <p class="userinfo">
    <% if current_user %>
      Signed in as <%= current_user.email %>. <%= link_to("New Picture", "/pictures/new") %> | <%= link_to "Log out", session_path("current"), method: :delete %>
    <% else %>
      <%= link_to "Log in", new_session_path %> or <%= link_to "Sign up", new_user_path %>
    <% end %>
  </p>
</body>
<%= yield %>

</body>
</html>
