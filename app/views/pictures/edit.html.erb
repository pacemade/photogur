<h1>Edit Picture</h1>

<p>
  <%= link_to("Back", "/pictures/#{@picture.id}") %>
</p>

<%= form_for(@picture, url: "/pictures/#{@picture.id}") do |f| %>
<%= render 'form_picture', picture: @picture, f: f %>
<% end %>
